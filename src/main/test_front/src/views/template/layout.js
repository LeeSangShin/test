import Header from "./header";
import Footer from "./footer";
// --- 아래는 예시 컴포넌트입니다. ---
const Layout = () => {
  return (
    <div>
      <Header />
      <Footer />
    </div>
  )
};

export default Layout;
